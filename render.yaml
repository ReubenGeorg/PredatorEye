services:
  - type: web
    name: predatoreye
    env: python
    region: oregon
    plan: free
    buildCommand: pip install -r web/requirements.txt
    startCommand: cd web && gunicorn app:app --bind 0.0.0.0:$PORT --workers 2
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.0"
