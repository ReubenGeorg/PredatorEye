[pytest]
testpaths = tests
addopts   = -v --tb=short
markers   =
    eicar: tests that write the EICAR test string to a temp file (may trigger AV)
    slow:  tests that run the full protection stack (takes a few seconds)
